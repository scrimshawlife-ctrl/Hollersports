# HollerSports standalone operator dependencies.
# Prefer installing the package (pulls runtime + optional dev extras):
#
#   pip install -e "packages/hollersports[dev]"
#
# Runtime deps are declared in packages/hollersports/pyproject.toml.
# Listed here for convenience when not using editable install.

-e packages/hollersports
pytest>=8.0
pytest-cov>=5.0
