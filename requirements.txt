# HollerSports Engine Requirements
# Python 3.8+

# Core dependencies (standard library only for now)
# The engine is designed to have minimal dependencies

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
